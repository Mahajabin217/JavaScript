// Using includes(), we can find out if an elements are in arrays:

const name = ['a','b','c','d','e'];
console.log(name.includes('c'));
console.log(name.includes('m'));

// Output: true
//         false